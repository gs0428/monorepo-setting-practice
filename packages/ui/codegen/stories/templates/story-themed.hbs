import type { Meta, StoryObj } from "@storybook/react";

import Themed{{pascalCase name}} from "@acme/ui/themed/{{name}}";

const meta = {
  title: "Themed-UI/{{pascalCase name}}",
  // 컴포넌트명 작성
  component: Themed{{pascalCase name}},
  tags: ["autodocs"],
  parameters: {
    // 컴포넌트 부제목 작성
    componentSubtitle: "버튼 컴포넌트",
    docs: {
      description: {
        // 컴포넌트 세부 설명 작성, props 등 상세한 설명 작성 (Optional)
        component: `- children으로 텍스트 값을 지정하면 됩니다.\n- onClick은 클릭 함수입니다.`
      },
    },
  },
  argTypes: {},
  // decorators (Optional)
  // 공통적으로 적용하고 싶은 컴포넌트 설정
  decorators: [
    (Story) => (
      <>
        {Story()}
      </>
    ),
  ],
} satisfies Meta<typeof Themed{{pascalCase name}}>;

export default meta;

type Story = StoryObj<typeof meta>;

/**
 * Story 설명 작성
 */
export const Primary: Story = {
  args: {},
};