import type { Meta, StoryObj } from "@storybook/react";

import Primitive{{pascalCase name}} from "@acme/ui/primitive/{{name}}";

const meta = {
  title: "Primitive-UI/{{pascalCase name}}",
  // 컴포넌트명 작성
  component: Primitive{{pascalCase name}},
  tags: ["autodocs"],
  parameters: {
    // 컴포넌트 부제목 작성
    componentSubtitle: "버튼 컴포넌트",
    docs: {
      description: {
        // 컴포넌트 세부 설명 작성, props 등 상세한 설명 작성 (Optional)
        component: `- children으로 텍스트 값을 지정하면 됩니다.\n- onClick은 클릭 함수입니다.`
      },
    },
  },
  argTypes: {},
  // decorators (Optional)
  // 공통적으로 적용하고 싶은 컴포넌트 설정
  decorators: [
    (Story) => (
      <>
        {Story()}
      </>
    ),
  ],
} satisfies Meta<typeof Primitive{{pascalCase name}}>;

export default meta;

type Story = StoryObj<typeof meta>;

/**
 * Story 설명 작성
 */
export const Primary: Story = {
  args: {},
};